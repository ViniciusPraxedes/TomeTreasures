version: 0.2

phases:
  pre_build:
    commands:
      - npm install
    build:
      commands:
        - npm run build
    post-build:
      commands:
        - aws s3 cp --recursive ./build s3://tome-treasures/